{
    "require": {
        "vlucas/phpdotenv": "^5.6"
    },
    "scripts": {
        "setup": [
            "composer install",
            "cd client && composer install",
            "cd server && composer install"
        ]
    }
}